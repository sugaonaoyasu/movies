<div class="main users-index">
    <div class="container">
        <h1 class="users-heading">ユーザー一覧</h1>
        <% @users.each do |user| %>
            <a href ="/users/<%= user.id %>"><%= user.name %></a>
        <% end %>
    </div>
</div>